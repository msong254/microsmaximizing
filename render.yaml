services:
  - type: web
    name: microsmaximizing-backend
    runtime: python
    buildCommand: ""
    startCommand: uvicorn server.main:app --host 0.0.0.0 --port 8000
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
    plan: free
